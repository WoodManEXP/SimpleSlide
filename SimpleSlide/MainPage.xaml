<Page
    x:Class="SimpleSlide.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleSlide"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="LightGray" KeyDown="KeyDownEvent">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel x:Name="PlaySpeedStackPanel" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
            <ProgressBar x:Name="PlaySpeedBar" Grid.Row="0" Width="200" Height="15"/>
            <TextBlock x:Name="PlaySpeedTextBlock" Grid.Row="0" Padding="5,5,5,5" HorizontalAlignment="Center"/>
        </StackPanel>
        <Image x:Name="Image1" Grid.Row="0">
            <!--
            Animation is happening, but duraion is 0 ?
            -->
            <Image.Triggers>
                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                    <BeginStoryboard>
                        <Storyboard x:Name="MyStoryboard">
                            <DoubleAnimation Storyboard.TargetName="Image1" 
                                             Storyboard.TargetProperty="Opacity" 
                                             From="0.0" To="1.0" Duration="0:0:2" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Image.Triggers>
        </Image>

        <Grid Grid.Row="1" VerticalAlignment="Bottom" Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel x:Name="OnScreenControls" Orientation="Horizontal" Visibility="Visible">
                <Button x:Name="SelectFolderBtn" Grid.Column="0" Grid.Row="0" Click="SelectFolderBtn_Click" Content="Select folder" Margin="3,3,3,3"
                ToolTipService.ToolTip="Select folder from which to begin (Ctrl-O)">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="O" Modifiers="Control"/>
                    </Button.KeyboardAccelerators>
                </Button>
                <Button x:Name="ContinuePauseBtn" Grid.Column="1"  Grid.Row="0" Content="Continue" Margin="3,3,3,3"
                Click="ContinuePauseBtn_Click" ToolTipService.ToolTip="Continue previous slide show">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="P" Modifiers="Control"/>
                    </Button.KeyboardAccelerators>
                </Button>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="2" Grid.Row="0">
                    <RadioButton x:Name="SlowSpeed_RB" GroupName="SpeedGroup" Content=" S" Click="SlowSpeed_RB_Click" FlowDirection="RightToLeft" MinWidth="25" />
                    <RadioButton x:Name="MediumSpeed_RB" GroupName="SpeedGroup" Content=" M" Click="MediumSpeed_RB_Click" FlowDirection="RightToLeft" IsChecked="True" MinWidth="25" />
                    <RadioButton x:Name="FastSpeed_RB" GroupName="SpeedGroup" Content=" F" Click="FastSpeed_RB_Click" FlowDirection="RightToLeft" MinWidth="25" />
                </StackPanel>
            </StackPanel>
            <TextBlock x:Name="StatusTextBlock" Grid.Column="3" Grid.Row="0" Padding="15,5,5,5" VerticalAlignment="Center" Text="FName"/>
        </Grid>
    </Grid>
</Page>
