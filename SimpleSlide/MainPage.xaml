<Page
    x:Class="SimpleSlide.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleSlide"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <!--<Rectangle Grid.Row="0"/>-->
        <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ProgressBar x:Name="ProgressBar" Grid.Row="0" Width="200" Height="15" IsIndeterminate="True" Visibility="Collapsed"/>
        </Viewbox>
        <Image x:Name="Image1" Grid.Row="0"></Image>
        <Grid Grid.Row="1" VerticalAlignment="Bottom" Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Button x:Name="SelectFolderBtn" Grid.Column="0" Grid.Row="0" Click="SelectFolderBtn_Click" Content="Select folder" Margin="3,3,3,3"
                ToolTipService.ToolTip="Select folder from which to begin"/>
            <Button x:Name="ContinuePauseBtn" Grid.Column="1"  Grid.Row="0" Content="Continue" Margin="3,3,3,3"
                Click="ContinuePauseBtn_Click" ToolTipService.ToolTip="Continue previous slide show"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="2" Grid.Row="0">
                <RadioButton x:Name="SlowSpeed_RB" GroupName="SpeedGroup" Content=" S" Click="SlowSpeed_RB_Click" FlowDirection="RightToLeft" MinWidth="25" />
                <RadioButton x:Name="MediumSpeed_RB" GroupName="SpeedGroup" Content=" M" Click="MediumSpeed_RB_Click" FlowDirection="RightToLeft" IsChecked="True" MinWidth="25" />
                <RadioButton x:Name="FastSpeed_RB" GroupName="SpeedGroup" Content=" F" Click="FastSpeed_RB_Click" FlowDirection="RightToLeft" MinWidth="25" />
            </StackPanel>
            <TextBlock x:Name="FNameTextBlock" Grid.Column="3" Grid.Row="0" Padding="15,5,5,5" VerticalAlignment="Center" Text="FName"/>
        </Grid>
    </Grid>
</Page>
